services:
  auth-service:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8006:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    networks:
      - task-management

networks:
  task-management:
    external: true
